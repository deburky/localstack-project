[project]
name = "localstack-project"
version = "0.1.0"
description = "LocalStack testing project"
requires-python = ">=3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
